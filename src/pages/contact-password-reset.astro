---
import Layout from '../layouts/Layout.astro';
import spamProtectionScript from '../scripts/spam-protection.js?raw';
---

<Layout 
	title="24/7 Password Reset Dallas Fort Worth Plano Arlington | Emergency Computer Unlock"
	description="Locked out of your computer? 24/7 emergency password reset in Dallas, Fort Worth, Plano, Arlington, Irving, Frisco. Windows & Mac PIN recovery. We come to you! Call (402) 416-6942."
	keywords="password reset dallas, password reset fort worth, computer unlock plano, emergency password recovery arlington, PIN reset irving, 24/7 computer unlock frisco, mckinney password reset"
>
	<!-- Full-Screen Split Contact Section -->
	<section class="split-contact">
		<div class="contact-background"></div>
		
		<!-- Left Side - Contact Information -->
		<div class="contact-left">
			<div class="contact-overlay"></div>
			<div class="left-content">
				<h1>24/7 EMERGENCY PASSWORD RESET</h1>
				<p class="emergency-badge">🔓 LOCKED OUT? WE'RE AVAILABLE NOW!</p>
				<p class="call-to-action">
					CALL FOR IMMEDIATE HELP: 
					<a href="tel:+14024166942" class="phone-number">(402) 416-6942</a>
				</p>
				<p class="or-text">OR REQUEST EMERGENCY SERVICE ONLINE</p>
				<div class="service-features">
					<div class="feature">✓ Available 24/7 - Nights & Weekends</div>
					<div class="feature">✓ Windows & Mac Password/PIN Reset</div>
					<div class="feature">✓ We Come To You - Same Day Service</div>
					<div class="feature">✓ No Data Loss - Files Stay Safe</div>
				</div>
				<p class="support-text">Don't panic! Our technicians can unlock your computer and reset your password without losing any data. Service available throughout Dallas-Fort Worth Metroplex.</p>
			</div>
		</div>
		
		<!-- Right Side - Mobile Device Form -->
		<div class="contact-right">
			<div class="mobile-device">
				<div class="device-top">
					<div class="device-camera"></div>
					<div class="device-speaker"></div>
					<div class="device-camera"></div>
				</div>
				
				<div class="device-screen">
					<div class="urgency-banner">🚨 EMERGENCY SERVICE REQUEST</div>
					<form class="mobile-form" id="contactForm" action="https://formsubmit.co/wellz.levi@gmail.com" method="POST">
						<!-- FormSubmit Configuration -->
						<input type="hidden" name="_subject" value="URGENT: 24/7 Password Reset Request from GadgetFix">
						<input type="hidden" name="_captcha" value="false">
						<input type="hidden" name="_template" value="table">
						<input type="hidden" name="service_type" value="Emergency Password Reset">
						
						<div class="form-field">
							<input type="text" id="fullName" name="name" placeholder="YOUR NAME" required>
						</div>
						
						<div class="form-field">
							<input type="tel" id="phoneNumber" name="phone" placeholder="PHONE NUMBER" required>
						</div>
						
						<div class="form-field">
							<input type="email" id="emailAddress" name="email" placeholder="EMAIL ADDRESS" required>
						</div>
						
						<div class="form-field">
							<select id="deviceType" name="device_type" required>
								<option value="">SELECT DEVICE TYPE</option>
								<option value="Windows Desktop">Windows Desktop</option>
								<option value="Windows Laptop">Windows Laptop</option>
								<option value="MacBook">MacBook</option>
								<option value="iMac">iMac</option>
								<option value="Mac Mini">Mac Mini</option>
								<option value="Microsoft Surface">Microsoft Surface</option>
								<option value="Other">Other Device</option>
							</select>
						</div>
						
						<div class="form-field">
							<select id="urgencyLevel" name="urgency" required>
								<option value="">HOW URGENT?</option>
								<option value="ASAP - Emergency">ASAP - Emergency (Need Help Now)</option>
								<option value="Today">Today - Within Few Hours</option>
								<option value="Tomorrow">Tomorrow Is Fine</option>
								<option value="This Week">Anytime This Week</option>
							</select>
						</div>
						
						<div class="form-field">
							<input type="text" id="location" name="location" placeholder="YOUR LOCATION (City/Area)" required>
						</div>
						
						<div class="form-field">
							<textarea id="additionalInfo" name="additional_info" placeholder="ADDITIONAL INFO (Optional - e.g., forgot password, PIN not working, account locked)" rows="3"></textarea>
						</div>
						
						<!-- Honeypot field - hidden from users, visible to bots -->
						<input type="text" name="website" style="display: none;" tabindex="-1" autocomplete="off">
						
						<!-- Cloudflare Turnstile -->
						<div class="cf-turnstile" 
							data-sitekey="0x4AAAAAABqyCtFg77TeYErI"
							data-theme="light"
							data-size="normal"
							style="margin: 15px 0;">
						</div>
						
						<button type="submit" class="send-button emergency-button">🔓 REQUEST EMERGENCY SERVICE</button>
					</form>
					<div id="formMessage" class="form-message"></div>
				</div>
			</div>
		</div>
	</section>

	<!-- Schema Markup for Emergency Service -->
	<script type="application/ld+json">
	{
		"@context": "https://schema.org",
		"@type": "Service",
		"serviceType": "Emergency Password Reset Service",
		"provider": {
			"@type": "LocalBusiness",
			"name": "GadgetFix",
			"telephone": "+14024166942",
			"priceRange": "$$",
			"areaServed": [
				"Dallas",
				"Fort Worth",
				"Plano",
				"Arlington",
				"Irving",
				"Frisco",
				"McKinney",
				"Garland",
				"Richardson",
				"Allen",
				"Carrollton",
				"Grand Prairie",
				"Mesquite",
				"Lewisville",
				"Flower Mound",
				"Denton",
				"Rockwall"
			]
		},
		"availableChannel": {
			"@type": "ServiceChannel",
			"serviceUrl": "https://gadgetfixdfw.com/contact-password-reset",
			"servicePhone": "+14024166942",
			"availableLanguage": "English"
		},
		"hoursAvailable": {
			"@type": "OpeningHoursSpecification",
			"dayOfWeek": [
				"Monday",
				"Tuesday",
				"Wednesday",
				"Thursday",
				"Friday",
				"Saturday",
				"Sunday"
			],
			"opens": "00:00",
			"closes": "23:59"
		}
	}
	</script>
</Layout>

<style>
	/* Full-Screen Split Contact Layout */
	.split-contact {
		height: 100vh;
		display: flex;
		position: relative;
		overflow: hidden;
	}

	/* Background Image */
	.contact-background {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: linear-gradient(135deg, rgba(220, 38, 38, 0.9) 0%, rgba(17, 24, 39, 0.8) 100%), 
					url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 1000"><defs><pattern id="lock" patternUnits="userSpaceOnUse" width="100" height="100"><rect fill="%23111827" width="100" height="100"/><circle fill="%23dc2626" opacity="0.1" cx="50" cy="50" r="3"/><path stroke="%23dc2626" opacity="0.2" d="M40 50 Q50 40 60 50 L60 70 L40 70 Z" stroke-width="1" fill="none"/></pattern></defs><rect fill="url(%23lock)" width="1000" height="1000"/></svg>') center/cover;
		z-index: 1;
	}

	/* Left Side - Contact Information */
	.contact-left {
		flex: 1;
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
		z-index: 2;
		padding: 2rem;
	}

	.contact-overlay {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: rgba(0, 0, 0, 0.4);
		z-index: -1;
	}

	.left-content {
		max-width: 500px;
		color: white;
		text-align: left;
	}

	.left-content h1 {
		font-family: var(--font-heading);
		font-size: 3.5rem;
		font-weight: 400;
		line-height: 1.1;
		margin-bottom: 1.5rem;
		text-transform: uppercase;
		letter-spacing: 2px;
	}

	.emergency-badge {
		background: #dc2626;
		color: white;
		padding: 10px 20px;
		border-radius: 50px;
		font-weight: 700;
		font-size: 1.1rem;
		display: inline-block;
		margin-bottom: 2rem;
		animation: pulse 2s infinite;
	}

	@keyframes pulse {
		0%, 100% {
			transform: scale(1);
		}
		50% {
			transform: scale(1.05);
		}
	}

	.call-to-action {
		font-size: 1.4rem;
		font-weight: 600;
		margin-bottom: 1.5rem;
		line-height: 1.3;
	}

	.phone-number {
		color: #fbbf24;
		text-decoration: none;
		font-weight: 700;
		font-size: 1.8rem;
		transition: color 0.3s ease;
		text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
	}

	.phone-number:hover {
		color: #fcd34d;
		text-decoration: underline;
	}

	.or-text {
		font-size: 1.2rem;
		font-weight: 600;
		margin-bottom: 2rem;
		opacity: 0.9;
	}

	.service-features {
		margin: 2rem 0;
		padding: 1.5rem;
		background: rgba(255, 255, 255, 0.1);
		border-radius: 15px;
		backdrop-filter: blur(10px);
	}

	.feature {
		font-size: 1.1rem;
		margin-bottom: 0.8rem;
		font-weight: 600;
		display: flex;
		align-items: center;
	}

	.feature:last-child {
		margin-bottom: 0;
	}

	.support-text {
		font-size: 1.1rem;
		line-height: 1.6;
		opacity: 0.9;
	}

	/* Right Side - Mobile Device Form */
	.contact-right {
		flex: 1;
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
		z-index: 2;
		padding: 2rem;
	}

	/* Mobile Device Container */
	.mobile-device {
		width: 100%;
		max-width: 400px;
		background: #1f2937;
		border-radius: 30px;
		padding: 20px;
		box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
		position: relative;
	}

	.device-top {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 20px;
		padding: 0 20px;
	}

	.device-camera {
		width: 12px;
		height: 12px;
		background: #374151;
		border-radius: 50%;
	}

	.device-speaker {
		width: 80px;
		height: 6px;
		background: #374151;
		border-radius: 3px;
	}

	/* Device Screen */
	.device-screen {
		background: white;
		border-radius: 20px;
		padding: 20px;
		min-height: 500px;
	}

	.urgency-banner {
		background: linear-gradient(135deg, #dc2626, #ef4444);
		color: white;
		padding: 12px;
		border-radius: 15px;
		text-align: center;
		font-weight: 700;
		font-size: 14px;
		margin-bottom: 20px;
		letter-spacing: 1px;
	}

	/* Mobile Form */
	.mobile-form {
		display: flex;
		flex-direction: column;
		gap: 15px;
		height: 100%;
	}

	.form-field {
		position: relative;
	}

	.form-field input,
	.form-field textarea,
	.form-field select {
		width: 100%;
		padding: 15px 20px;
		border: 2px solid #e5e7eb;
		border-radius: 25px;
		font-size: 16px;
		font-weight: 500;
		background: #f9fafb;
		transition: all 0.3s ease;
		resize: none;
		box-sizing: border-box;
	}

	.form-field input:focus,
	.form-field textarea:focus,
	.form-field select:focus {
		outline: none;
		border-color: #dc2626;
		background: white;
		box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.1);
	}

	.form-field input::placeholder,
	.form-field textarea::placeholder {
		color: #9ca3af;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 1px;
		font-size: 14px;
	}

	.form-field textarea {
		border-radius: 20px;
		min-height: 80px;
		font-family: inherit;
	}

	/* Emergency Send Button */
	.emergency-button {
		background: linear-gradient(135deg, #dc2626, #ef4444);
		color: white;
		border: 2px solid #dc2626;
		padding: 18px 40px;
		border-radius: 100px;
		font-size: 16px;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 2px;
		cursor: pointer;
		transition: all 0.3s ease;
		margin-top: 10px;
		animation: glow 2s ease-in-out infinite;
	}

	@keyframes glow {
		0%, 100% {
			box-shadow: 0 0 20px rgba(220, 38, 38, 0.3);
		}
		50% {
			box-shadow: 0 0 30px rgba(220, 38, 38, 0.5);
		}
	}

	.emergency-button:hover {
		background: linear-gradient(135deg, #b91c1c, #dc2626);
		border-color: #b91c1c;
		transform: translateY(-2px);
		box-shadow: 0 10px 25px rgba(220, 38, 38, 0.3);
	}

	.emergency-button:active {
		transform: translateY(0);
	}

	/* Form Message */
	.form-message {
		margin-top: 15px;
		padding: 12px;
		border-radius: 10px;
		text-align: center;
		font-weight: 600;
		display: none;
	}

	.form-message.success {
		background: #10b981;
		color: white;
	}

	.form-message.error {
		background: #ef4444;
		color: white;
	}

	/* Responsive Design */
	@media (max-width: 1024px) {
		.split-contact {
			flex-direction: column;
			height: auto;
			min-height: 100vh;
		}

		.contact-left {
			flex: none;
			padding: 4rem 2rem 2rem;
			text-align: center;
		}

		.contact-right {
			flex: none;
			padding: 2rem 2rem 4rem;
		}

		.left-content h1 {
			font-size: 2.8rem;
		}

		.service-features {
			text-align: left;
			max-width: 400px;
			margin: 2rem auto;
		}

		.call-to-action {
			font-size: 1.2rem;
		}

		.phone-number {
			font-size: 1.6rem;
		}
	}

	@media (max-width: 768px) {
		.split-contact {
			padding: 0;
		}

		.contact-left {
			padding: 3rem 1.5rem 2rem;
		}

		.contact-right {
			padding: 2rem 1.5rem 3rem;
		}

		.left-content h1 {
			font-size: 2.2rem;
			letter-spacing: 1px;
		}

		.emergency-badge {
			font-size: 1rem;
			padding: 8px 16px;
		}

		.call-to-action {
			font-size: 1.1rem;
		}

		.phone-number {
			font-size: 1.4rem;
		}

		.feature {
			font-size: 1rem;
		}

		.or-text {
			font-size: 1.1rem;
		}

		.support-text {
			font-size: 1rem;
		}

		.mobile-device {
			max-width: 350px;
			padding: 15px;
		}

		.device-screen {
			padding: 15px;
			min-height: 450px;
		}

		.urgency-banner {
			font-size: 12px;
			padding: 10px;
		}
	}

	@media (max-width: 480px) {
		.left-content h1 {
			font-size: 1.8rem;
		}

		.emergency-badge {
			font-size: 0.9rem;
		}

		.call-to-action {
			font-size: 1rem;
		}

		.phone-number {
			font-size: 1.3rem;
		}

		.feature {
			font-size: 0.95rem;
			margin-bottom: 0.6rem;
		}

		.service-features {
			padding: 1rem;
		}

		.mobile-device {
			max-width: 320px;
			border-radius: 25px;
			padding: 12px;
		}

		.device-screen {
			padding: 15px;
			min-height: 400px;
			border-radius: 15px;
		}

		.form-field input,
		.form-field textarea,
		.form-field select {
			padding: 12px 16px;
			font-size: 14px;
		}

		.emergency-button {
			padding: 15px 25px;
			font-size: 14px;
		}
	}
</style>

<script define:vars={{ spamProtectionScript }}>
	// Add spam protection script
	eval(spamProtectionScript);
</script>

<script>
	document.addEventListener('DOMContentLoaded', function() {
		const form = document.getElementById('contactForm');
		const formMessage = document.getElementById('formMessage');
		let lastSubmitTime = 0;
		const RATE_LIMIT_MS = 30000; // 30 seconds between submissions
		
		// Initialize spam protection
		let spamProtection;
		if (form) {
			spamProtection = new SpamProtection(form);
			console.log('SpamProtection initialized');
		}
		
		if (form) {
			form.addEventListener('submit', async function(e) {
				e.preventDefault(); // Always prevent default to handle submission via AJAX
				
				// Check rate limiting
				const now = Date.now();
				if (now - lastSubmitTime < RATE_LIMIT_MS) {
					const secondsLeft = Math.ceil((RATE_LIMIT_MS - (now - lastSubmitTime)) / 1000);
					console.log(`Rate limited: ${secondsLeft} seconds remaining`);
					formMessage.style.color = '#ef4444';
					formMessage.textContent = `Please wait ${secondsLeft} seconds before submitting again.`;
					formMessage.style.display = 'block';
					return false;
				}
				
				// Check honeypot field
				const honeypot = form.querySelector('input[name="website"]');
				if (honeypot && honeypot.value.trim() !== '') {
					console.log('Spam blocked: honeypot field filled');
					formMessage.style.color = '#ef4444';
					formMessage.textContent = 'Form submission blocked. Please call (402) 416-6942.';
					formMessage.style.display = 'block';
					return false;
				}
				
				// Log SpamProtection metadata
				if (spamProtection) {
					const metadata = spamProtection.getMetadata();
					console.log('SpamProtection metadata:', metadata);
				}
				
				// Get button and change text
				const submitButton = form.querySelector('.send-button');
				const originalText = submitButton.textContent;
				submitButton.textContent = 'SENDING...';
				submitButton.disabled = true;
				
				try {
					// Get Turnstile token if available
					const turnstileResponse = form.querySelector('[name="cf-turnstile-response"]');
					if (turnstileResponse && !turnstileResponse.value) {
						console.log('Turnstile not completed');
						formMessage.style.color = '#ef4444';
						formMessage.textContent = 'Please complete the security check.';
						formMessage.style.display = 'block';
						submitButton.textContent = originalText;
						submitButton.disabled = false;
						return false;
					}
					
					// Submit via AJAX to FormSubmit
					const formData = new FormData(form);
					const response = await fetch('https://formsubmit.co/ajax/wellz.levi@gmail.com', {
						method: 'POST',
						headers: {
							'Accept': 'application/json'
						},
						body: formData
					});
					
					const result = await response.json();
					
					if (response.ok) {
						// Success - show message and reset form
						formMessage.className = 'form-message success';
						formMessage.textContent = 'Emergency request sent! We\'ll call you within 30 minutes.';
						formMessage.style.display = 'block';
						form.reset();
						lastSubmitTime = now; // Update rate limit timer
						
						// Hide message after 5 seconds
						setTimeout(() => {
							formMessage.style.display = 'none';
						}, 5000);
					} else {
						throw new Error('Failed to send');
					}
				} catch (error) {
					console.error('Form submission error:', error);
					formMessage.className = 'form-message error';
					formMessage.textContent = 'Error sending request. Please call (402) 416-6942 directly.';
					formMessage.style.display = 'block';
				} finally {
					submitButton.textContent = originalText;
					submitButton.disabled = false;
				}
			});
		}
	});
</script>